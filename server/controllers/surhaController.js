// import Surah from "../models/Surah.js";
// export const createSurah = async (req, res, next) => {
//   const { pageId, surahData } = req.pageInfo;
//   const { surahName } = surahData;
//   const surah = await Surah.create({ surahName, pages: [pageId] });
//   req.JuzInfo = { surahId: surah._id };
//   next();
// };
